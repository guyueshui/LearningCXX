#include <iostream>
#include <vector>

using namespace std;

int MinStep(const vector<int>& buckets, int target)
{
  
}

int main()
{
  vector<int> buckets(3, 0);
  cin >> buckets[0] >> buckets[1] >> buckets[2];
  int target;
  cin >> target;
  // io done
  return 0;
}
