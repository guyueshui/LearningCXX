/*
 * Ex 9.35
 *   - capacity: the memory space a vector has
 *   - size: the current number of elems that the vector has
 * 
 * Ex 9.36
 *   capacity >= size
 *
 * Ex 9.37
 *   - list: list doesn't store elems in continuous memory
 *     space
 *   - array: array is implemented for an alternative for the
 *     built-in array, we can't even change its size
 */
