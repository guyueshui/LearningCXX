/*
 * In general, `!=` is suppported by more types than '<', while function
 * template had better to has as few as possible requirements to the
 * template parameter. So we prefer `!=` instead of `<` or `>`.
 */
