#include <iostream>
#include <vector>

using namespace std;

int main()
{
  int num;
  cin >> num;
  vector<int> arr;
  for (int i = 0, tmp = 0; i != num; ++i)
  {
    cin >> tmp;
    arr.push_back(tmp);
  }

  vector<int> ret;
  for (int len = 1; len <= num; ++len)
  {
    int min = 0;
    for () 
  }
}
